# Never bake secrets or local env into the image
**/.env
**/.env.*

# Deps are reinstalled in the image
**/node_modules

# Not needed by the image (apps/web IS needed now — its build is baked in)
.git
.gitignore
packages/shared/node_modules
*.zip
Provided Images/
**/.DS_Store
*.log
